# `cj-homes` 服务器部署流程

这份文档用于下次直接执行，不需要再在对话里重新确认上下文。

适用目标：

- Git 仓库：`http://159.75.51.166:3000/admin/cj-homes`
- 部署分支：`main`
- 服务器：`ubuntu@159.75.51.166`
- 应用验收地址：`http://159.75.51.166:8090/`
- 当前容器名：`homes`
- 当前镜像名：`cj-homes:latest`

## 1. 本次线上环境约定

服务器上当前服务不是用 `docker compose` 管理的，而是直接 `docker run` 起的。

当前挂载关系：

- `/home/ubuntu/apps/cj-homes/data/homes_sql` -> `/app/Backend-System/sql`
- `/home/ubuntu/apps/cj-homes/data/homes_cfg` -> `/app/Backend-System/config`
- `/home/ubuntu/apps/cj-homes/data/homes_uploads` -> `/app/Backend-System/static/uploads`

当前端口映射：

- `8090:80`

## 2. 部署前准备

### 2.1 SSH 私钥

确保本机私钥在下面路径之一：

```bash
/Users/yanzimmer/ai_demo_ed25519
```

并修正权限：

```bash
chmod 600 /Users/yanzimmer/ai_demo_ed25519
```

### 2.2 仓库账号

仓库使用 Gitea Deploy Token 访问，执行命令前将下方的
`GITEA_DEPLOY_TOKEN_VALUE` 替换成当前有效的只读 Deploy Token。不要把真实
Token 写入本文档或提交到仓库。

## 3. 一次完整部署

### 3.1 确认远端 `main` 最新提交

```bash
git ls-remote http://admin:GITEA_DEPLOY_TOKEN_VALUE@159.75.51.166:3000/admin/cj-homes.git refs/heads/main
```

记下输出里的提交号，例如：

```text
446284c580303185fae9292d81f61889bcfc4826	refs/heads/main
```

### 3.2 检查服务器当前运行状态

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
sudo docker ps --filter name=^/homes$ --format "table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}";
echo ---;
sudo docker images --format "table {{.Repository}}\t{{.Tag}}\t{{.ID}}\t{{.CreatedSince}}" | grep "cj-homes"
'
```

### 3.3 拉取服务器最新代码

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
set -e;
rm -rf /home/ubuntu/tmp-cj-homes-deploy;
git clone --depth 1 --branch main http://admin:GITEA_DEPLOY_TOKEN_VALUE@127.0.0.1:3000/admin/cj-homes.git /home/ubuntu/tmp-cj-homes-deploy;
cd /home/ubuntu/tmp-cj-homes-deploy;
git rev-parse HEAD
'
```

说明：

- 这里在服务器内部用 `127.0.0.1:3000` 访问 Gitea，更稳。
- 拉完后请确认提交号和 `3.1` 一致。

### 3.4 给当前线上镜像打备份标签

把时间和提交号改成当时实际值：

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
sudo docker tag cj-homes:latest cj-homes:pre-446284c-20260722-1617
'
```

### 3.5 生成发布用 `Dockerfile.release`

原因：

- 原始 `Dockerfile` 运行时阶段会重新 `apt-get` 和 `pip install`
- 服务器对外拉源不稳定，之前在这一步卡住过
- 更稳的做法是以“当前可运行的线上镜像”为底座，只替换最新代码和前端构建产物

执行：

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
set -euo pipefail;
cd /home/ubuntu/tmp-cj-homes-deploy;
cat > Dockerfile.release <<'"'"'EOF'"'"'
FROM node:22-alpine AS frontend-builder
WORKDIR /frontend
COPY homes-frontend/package*.json ./
RUN npm ci
COPY homes-frontend ./
ENV VITE_API_BASE_URL=/api
RUN npm run build

FROM cj-homes:pre-446284c-20260722-1617
WORKDIR /app
COPY Backend-System /app/Backend-System
COPY --from=frontend-builder /frontend/dist /usr/share/nginx/html
COPY deploy/nginx.conf /etc/nginx/conf.d/default.conf
COPY deploy/supervisord.conf /etc/supervisor/conf.d/supervisord.conf
RUN mkdir -p /app/Backend-System/sql /var/log/supervisor
EOF
'
```

注意：`FROM cj-homes:pre-446284c-20260722-1617` 要替换成你刚才打的备份标签。

### 3.6 构建最新镜像

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
set -euo pipefail;
cd /home/ubuntu/tmp-cj-homes-deploy;
sudo docker build -f Dockerfile.release -t cj-homes:latest .
'
```

### 3.7 替换容器并重启

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
set -euo pipefail;
sudo docker rm -f homes;
sudo docker run -d \
  --name homes \
  --restart unless-stopped \
  -p 8090:80 \
  -v /home/ubuntu/apps/cj-homes/data/homes_sql:/app/Backend-System/sql \
  -v /home/ubuntu/apps/cj-homes/data/homes_cfg:/app/Backend-System/config \
  -v /home/ubuntu/apps/cj-homes/data/homes_uploads:/app/Backend-System/static/uploads \
  -e TZ=Asia/Shanghai \
  cj-homes:latest \
  /usr/bin/supervisord -n -c /etc/supervisor/conf.d/supervisord.conf
'
```

### 3.8 验收

先看容器状态：

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
sudo docker ps --filter name=^/homes$ --format "table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}"
'
```

再看外部访问：

```bash
curl -I --max-time 10 http://159.75.51.166:8090/
curl -I --max-time 10 http://159.75.51.166:8090/admin/cj-homes
```

成功标准：

- 两个地址都返回 `HTTP/1.1 200 OK`
- 容器最终变为 `healthy`

健康状态单查：

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
sudo docker inspect homes --format "Status={{.State.Status}} Health={{if .State.Health}}{{.State.Health.Status}}{{else}}none{{end}}"
'
```

## 4. 快速回滚

如果新版本有问题，把镜像换回上一个备份标签即可。

示例：

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
set -euo pipefail;
sudo docker rm -f homes;
sudo docker run -d \
  --name homes \
  --restart unless-stopped \
  -p 8090:80 \
  -v /home/ubuntu/apps/cj-homes/data/homes_sql:/app/Backend-System/sql \
  -v /home/ubuntu/apps/cj-homes/data/homes_cfg:/app/Backend-System/config \
  -v /home/ubuntu/apps/cj-homes/data/homes_uploads:/app/Backend-System/static/uploads \
  -e TZ=Asia/Shanghai \
  cj-homes:pre-446284c-20260722-1617 \
  /usr/bin/supervisord -n -c /etc/supervisor/conf.d/supervisord.conf
'
```

## 5. 本项目已确认过的关键坑

### 5.1 `Dockerfile` 直接构建可能卡在外网依赖

现象：

- `docker build` 卡在 `apt-get update`
- 或卡在拉 `node:22-alpine`

建议：

- 默认不要直接用原始 `Dockerfile` 发版
- 优先使用本文件里的 `Dockerfile.release` 方案

### 5.2 `nginx.conf` 不能带 BOM

之前遇到过：

```text
unknown directive "﻿server" in /etc/nginx/conf.d/default.conf:1
```

原因：

- 文件头带了 UTF-8 BOM

现在远端仓库里的 `deploy/nginx.conf` 已经是正常的，但如果以后再出现这个错误，先检查编码。

### 5.3 线上上传恢复备份依赖 `client_max_body_size`

当前线上 `deploy/nginx.conf` 需要包含：

```nginx
client_max_body_size 200m;
```

如果没有，备份上传可能失败。

### 5.4 备份恢复时不能删除 Docker 挂载点本身

之前错误是：

```text
[Errno 16] Device or resource busy: '/app/Backend-System/config'
[Errno 16] Device or resource busy: '/app/Backend-System/static/uploads'
```

原因：

- 这两个目录是 Docker bind mount
- 如果代码对挂载点本身执行 `shutil.rmtree(...)`，就会报错

当前正确实现应该是这种逻辑：

```python
def _clear_directory(directory):
    os.makedirs(directory, exist_ok=True)
    for entry in os.scandir(directory):
        target_path = entry.path
        if entry.is_dir(follow_symlinks=False):
            shutil.rmtree(target_path)
        else:
            os.remove(target_path)
```

如果以后恢复备份再次报这个错，先检查远端最新代码里这段实现是不是还在。

## 6. 常用排查命令

### 6.1 看容器日志

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
sudo docker logs --tail 200 homes
'
```

### 6.2 看容器挂载

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
sudo docker inspect homes --format "{{json .Mounts}}"
'
```

### 6.3 看容器内配置目录

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 '
sudo docker exec homes sh -lc "ls -la /app/Backend-System/config"
'
```

### 6.4 看运行中的代码是否包含修复

```bash
ssh -i /Users/yanzimmer/ai_demo_ed25519 -o IdentitiesOnly=yes ubuntu@159.75.51.166 "
sudo docker exec homes python -c 'from pathlib import Path; text=Path(\"/app/Backend-System/system_api.py\").read_text(encoding=\"utf-8\"); start=text.find(\"def _clear_directory(directory):\"); print(text[start:start+260])'
"
```

## 7. 下次最短执行顺序

如果只是正常发最新版，按下面顺序即可：

1. 查远端 `main` 最新提交
2. `git clone` 到 `/home/ubuntu/tmp-cj-homes-deploy`
3. 给当前 `cj-homes:latest` 打一个 `pre-...` 备份标签
4. 生成 `Dockerfile.release`
5. `docker build -f Dockerfile.release -t cj-homes:latest .`
6. 删除并重建 `homes` 容器
7. 验收 `http://159.75.51.166:8090/`

## 8. 建议

如果后面想继续降低人工操作，下一步最值得做的是：

1. 把这套流程写成服务器脚本，例如 `/home/ubuntu/deploy-cj-homes.sh`
2. 把仓库账号改成 deploy token，避免文档里长期写明文密码
3. 把当前 `docker run` 固化成 `docker compose.yml`，减少参数手抄风险
